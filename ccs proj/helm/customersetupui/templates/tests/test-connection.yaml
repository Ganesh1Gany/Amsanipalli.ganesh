apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "customersetupui.fullname" . }}-test-connection"
  labels:
    {{- include "customersetupui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "customersetupui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
