apiVersion: v1
kind: Service
metadata:
  name: {{ include "customersetupui.fullname" . }}
  labels:
    {{- include "customersetupui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "customersetupui.selectorLabels" . | nindent 4 }}
